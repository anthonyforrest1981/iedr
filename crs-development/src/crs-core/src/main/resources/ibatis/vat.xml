<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
        PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
        "sql-map-2.dtd">

<sqlMap namespace="vat">

    <cacheModel id="vatCache" type="LRU">
        <flushInterval hours="24" />
        <property name="size" value="1000" />
    </cacheModel>

    <resultMap id="vatResult" class="pl.nask.crs.vat.Vat">
        <result property="id" column="id" />
        <result property="category" column="category"
            typeHandler="pl.nask.crs.commons.dao.ibatis.handlers.StringHandler" />
        <result property="fromDate" column="fromDate" />
        <result property="vatRate" column="vatRate" />
    </resultMap>

    <select id="getVatCategory" cacheModel="vatCache"
        resultMap="common.normalizedString">
        SELECT
        code
        FROM
        VatCategory
    </select>

    <select id="getCurrentVat" cacheModel="vatCache"
        parameterClass="java.util.Map" resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as fromDate,
        Rate as vatRate
        FROM
        Vat
        WHERE
        Category =
        #category,handler=pl.nask.crs.commons.dao.ibatis.handlers.StringHandler#
        AND
        From_Dt <![CDATA[<=]]>
        #forDate,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#
        AND
        Valid = 'YES'
        ORDER BY From_Dt DESC
        LIMIT 1
    </select>

    <select id="getCurrentAndPreviousVatList" cacheModel="vatCache"
        parameterClass="java.util.Date" resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as fromDate,
        Rate
        as vatRate
        FROM
        Vat
        WHERE
        From_Dt <![CDATA[<=]]>
        #forDate,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#
        AND
        Valid = 'YES'
    </select>

    <select id="getVatsForCategoryAndFrom" parameterClass="java.util.Map"
        resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as fromDate,
        Rate as vatRate
        FROM
        Vat
        WHERE
        Category = #category,handler=pl.nask.crs.commons.dao.ibatis.handlers.StringHandler#
        AND
        From_Dt = DATE_FORMAT(#from,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#, '%Y-%m-%d')
    </select>

    <select id="getCurrentVatExceptVatId" parameterClass="java.util.Map"
        resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as fromDate,
        Rate
        as vatRate
        FROM
        Vat
        WHERE
        Category = #category,handler=pl.nask.crs.commons.dao.ibatis.handlers.StringHandler#
        AND
        From_Dt <![CDATA[<=]]> #forDate,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#
        AND
        Valid = 'YES'
        AND
        ID != #vatId#
        ORDER BY From_Dt DESC
        LIMIT 1
    </select>

    <select id="getVatById" parameterClass="int" resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as fromDate,
        Rate
        as vatRate
        FROM
        Vat
        WHERE
        ID = #id#
    </select>

    <insert id="create" parameterClass="pl.nask.crs.vat.Vat">
        INSERT INTO Vat
        (Category,
        From_Dt,
        Rate,
        Valid)
        VALUES
        (#category,handler=pl.nask.crs.commons.dao.ibatis.handlers.StringHandler#,
        #fromDate,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#,
        #vatRate#,
        'YES');
        <selectKey keyProperty="id" resultClass="int">
            SELECT
            LAST_INSERT_ID();
        </selectKey>
    </insert>

    <update id="invalidate" parameterClass="int">
        UPDATE Vat SET
        Valid =
        'NO'
        WHERE
        ID = #vatId#
    </update>

    <select id="getAllValid" resultMap="vatResult">
        SELECT
        ID as id,
        Category as
        category,
        From_Dt as fromDate,
        Rate as vatRate
        FROM
        Vat
        WHERE
        Valid = 'YES'
        ORDER BY Category,From_Dt DESC
    </select>

    <select id="getCurrentAndFutureVatList" parameterClass="java.util.Date"
        resultMap="vatResult">
        SELECT
        ID as id,
        Category as category,
        From_Dt as
        fromDate,
        Rate as vatRate
        FROM
        Vat
        WHERE
        From_Dt >=
        #forDate,handler=pl.nask.crs.commons.dao.ibatis.handlers.TruncatedDateHandler#
        AND
        Valid = 'YES'
    </select>
</sqlMap>

